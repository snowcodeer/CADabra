# Copy this file to backend/.env and fill in real values.
# backend/.env is gitignored; this template is the only env file we commit.

# Anthropic API key for Claude Vision (used by backend/ai_infra/llm_client.py).
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...
